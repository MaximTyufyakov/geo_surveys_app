name: geo_surveys_app
description: "Mobile app for geocontrol surveys"
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.5.4

dependencies:
  bcrypt: ^1.1.3
  camera: ^0.11.2
  flutter:
    sdk: flutter
  postgres_dart: ^1.1.3
  provider: ^6.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
